name: First Env Vars

on:
  release:
    types: [created]
#on: [push]

jobs:
  run_github_actions:
    runs-on: ubuntu-20.04
    steps:
      - name: ls before
        run: |
          pwd
          ls -a
      - name: checkout
        uses: actions/checkout@main
      - name: ls after
        run: |
          pwd
          ls -a
